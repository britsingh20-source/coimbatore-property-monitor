# Optional audio

Add an optional narration or licensed music file named `<video-id>.mp3` or
`<video-id>.wav`. Record your own Tamil voice-over for the best free result.
If no audio is present, the renderer creates a silent captioned video.
